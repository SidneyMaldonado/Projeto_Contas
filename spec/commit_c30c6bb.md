# commit c30c6bb (merge)

- **Data:** 2026-07-20
- **Autor:** Sidney Maldonado
- **Mensagem original:** Merge pull request #1 from SidneyMaldonado/rename-contas-biz-to-core ("Rename contas biz to core")

## Resumo

Commit de merge do PR #1 (`rename-contas-biz-to-core`) em `master`. Traz os dois commits da branch:

- `d0b5447` — Renomeia projeto `Contas_Biz` para `Contas_Core`.
- `18610e7` — Adiciona o CHECK constraint ausente de `tb_conta.nr_saldo` no script SQL.

Sem alterações de conteúdo próprias além da junção das duas branches (merge commit padrão do GitHub, sem `-m`/diff adicional).
